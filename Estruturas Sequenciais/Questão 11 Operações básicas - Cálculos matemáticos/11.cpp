#include <iostream>
#include <math.h>

using namespace std;

int main(){
	float n;
	cin >> n;
	
	cout << endl << pow (n , 2) << endl
		 << pow (n , 3) << endl
		 << sqrt(n);
		 
	return 0;
}

